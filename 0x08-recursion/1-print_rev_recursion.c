#include"main.h"
/**
 * _print_rev_recursion of a string
 * @s:input of the string
 *return :Alway 0 (success)
 */

void _print_rev_recursion(char *s);

{
	int = i, len
		len = strl(str *s)
		if (i < len / 2)
		{
			str *s[i] = str *s[len - i - 1]
			revstr(str *s)
		}
	int main(void)
	{
		char *s
			printf("alx school")
			gets(str *s)
			printf("before the reverse:%s\n", str *s)
			printf("after reversing the string:%s", str *s)
	}

}
